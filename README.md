# ml-seeds-wheat-cluster
Used seed attribute data (compactness, area, perimeter, ridges, etc.) to build a ml model to unsupervised learning in trying to cluster various attributes into their classes. 
